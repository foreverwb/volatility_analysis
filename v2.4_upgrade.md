# 期权策略量化分析系统 v2.4  
—— 结构升级与治理说明

> 基于对 v2.3.3 结构审计后的“治理型升级”，目标是：  
> **减少有效自由度、削弱因子冗余、移除结构性污染源、解耦事件与决策、内嵌审计与约束机制。**:contentReference[oaicite:0]{index=0}

---

## 1. 版本背景与设计目标

### 1.1 v2.3.3 结构性问题回顾

在 v2.3.3 中，系统已具备：

- 多源数据输入（期权量价、IV/HV、VIX、结构比例等）  
- DirScore / VolScore 双层评分体系  
- 三层动态参数（βₜ 行为层、λₜ 波动层、αₜ 市场层）  
- 结构置信度、跨期一致性、流动性与缺失惩罚等多重修正  
- 基于四象限的策略建议矩阵（偏多/偏空 × 买波/卖波）:contentReference[oaicite:1]{index=1}  

结构审计发现的关键风险点包括：

1. **同源因子多重表达**：  
   - 方向侧多种成交量/名义/比率/偏度同时入模；  
   - 波动侧对相同 IV/HV 关系进行了多种函数变换并独立计分。:contentReference[oaicite:2]{index=2}  

2. **动态参数与评分输入高度同源**：  
   - βₜ 与 DirScore 使用高度重叠的底层变量族；  
   - λₜ 与 VolScore 共享 IV30/HV20 族的多种派生形式。:contentReference[oaicite:3]{index=3}  

3. **修正层乘性叠加过多**：  
   - 结构、跨期、流动性、恐慌惩罚、缺失惩罚等以乘法链路叠加在分数与强度上。:contentReference[oaicite:4]{index=4}  

4. **缺失值常数填充（尤其波动率填 0）为结构性污染源**：  
   - 对比率/对数/折价项产生非经济含义的极端输入。:contentReference[oaicite:5]{index=5}  

5. **Gamma Squeeze 事件模块对策略有“覆盖式”指导**：  
   - 以“强烈建议 Long Call”形式直接影响策略输出。:contentReference[oaicite:6]{index=6}  

6. **整体有效自由度偏高**：  
   - 阈值、窗口、权重、分段规则与动态参数配置项较多。

### 1.2 v2.4 设计目标

v2.4 的设计目标是把上述问题转化为**结构级别的改动**，而非调参：

1. **因子空间重构**：  
   - 从“字段视角”转为“子空间视角”，减少同源因子多重表达。  

2. **动态参数解耦与收缩**：  
   - 动态参数仅依赖高层聚合因子，而非底层原始变量；  
   - 对动态参数引入“预算”与收缩机制。  

3. **修正层瘦身与单通道化**：  
   - 各种修正只通过“置信度通道”生效，避免多重乘性链路。  

4. **事件模块降级为标签层**：  
   - 不再直接给出强制策略模板，只提供风险/机会标签。  

5. **缺失值处理结构化**：  
   - 从“填数”转为“缺失标签 + 置信度约束”，避免污染输入分布。  

6. **治理与审计可操作化**：  
   - 明确 v2.4 的结构审计清单，将“可治理性”写入版本规范。

---

## 2. 版本对比概览

### 2.1 结构变更总览

| 维度 | v2.3.3 | v2.4 升级方向 |
|------|--------|---------------|
| 缺失值处理 | 常数填充（波动率填 0 等） | 结构化缺失：缺失标签 + 置信度降权 / 决策拒绝 |
| 方向信号 | 多种量价/名义/偏度/比率并行叠加 | 因子子空间重构，减少同源表达，显式正交化 |
| 波动信号 | 同一 IV/HV 关系多种函数形式并行入模 | 压缩为少数“相对波动 + 价位 + 期限结构”主因子 |
| 动态参数 βₜ / λₜ | 直接依赖与评分重叠的底层变量 | 改为依赖高层子空间因子，增加收缩与预算约束 |
| 修正层 | 多重乘性叠加（结构/跨期/流动性/缺失等） | 统一到单一置信度通道，禁止直接改写分数 |
| 事件（Gamma Squeeze） | 可直接给出强烈策略建议 | 降级为标签，仅影响置信度上限内的小幅修正 |
| 决策映射 | 刚性阈值 → 四象限 → 策略模板 | 引入中性缓冲带；象限与策略模板解耦 |

---

## 3. 数据与预处理模块升级（v2.4）

### 3.1 缺失值处理重构

**变更点**

- **移除**：对波动率类（IV/HV）、金额类、比率类因子的常数填充（如 IV30/HV20 填 0）。  
- **新增**：缺失值处理改为“结构化缺失机制”：
  1. 为每类关键字段族（波动率、量价、结构等）增加对应的“缺失标记因子”；  
  2. 当关键字段缺失时：  
     - 该字段不参与因子计算；  
     - 对全局置信度施加下限约束/惩罚；  
     - 必要时触发“决策拒绝（no-op）”而非给出低质量信号。  

**设计动机**

- 避免用数值伪造本不存在的市场信息；  
- 明确把“数据质量”从信号内容中剥离出来，独立进入治理通道。

**治理约束**

- 任一核心因子若依赖字段缺失，只能通过置信度层反映，**禁止**以猜测值/默认值替代。  
- 审计时需能查询：某一输出对应的“缺失字段列表”。

---

## 4. 信号层升级（DirScore / VolScore）

### 4.1 因子子空间重构（方向信号）

**变更点**

1. 将方向相关因子划分为三个**正交子空间**：
   - **价格/趋势子空间**：价格变动、趋势平滑等；  
   - **订单流/名义子空间**：成交量、名义金额、订单流偏度、主动开仓强度等；  
   - **结构/参与形态子空间**：单腿、多腿、联动、指数 vs 个股结构性差异。  

2. 在每个子空间内，仅保留**1–2 个聚合后的主因子**，替代原有多条类似规则叠加。  
   - 例如：将多种量/名义/偏度指标压缩为一个“订单流方向强度因子”。  

3. 明确禁止以下模式：
   - 同一底层字段的多种单调变换同时以**独立加权**方式入模；  
   - 通过额外分段规则重复表达已反映过的信息。

**设计动机**

- 降低因子共线性，减小有效自由度；  
- 保证每个因子的“信息含义”可解释且不重复。

**治理约束**

- 每个子空间的因子清单须有**完整说明文档**，列明：  
  - 底层字段集合；  
  - 聚合逻辑（仅描述结构，不要求公开具体函数形式）；  
  - 与其他子空间的依赖关系（必须为“无/弱”）。  
- 对任何新因子的加入，必须同时说明“归属子空间”与“额外信息增量”的论证口径。

### 4.2 相对波动与价位因子的收敛（波动信号）

**变更点**

1. 将 IV/HV 相关指标统一归入一个**相对波动主因子**：  
   - IV30/HV20 比率、其对数形式、折价项等不再独立计分，而是被整合为单一主因子。  

2. 将 IVR 相关指标归入单一“波动价位因子”；  
   - 禁止同时使用“IVR 中心化”“IVR 高/低触发”等多个独立项。  

3. 期限结构因子（IV30/IV90）独立为一个“期限形态因子”；  
   - 仅通过一个形态分类/分数表达，不再设置多组重叠的分段规则。  

4. 财报/事件/恐慌等环境因素，只能以**加性修饰因子**形式存在，且受上限约束：  
   - 禁止多重 if 条件叠加产生过大的离散跳变。

**设计动机**

- 避免对同一 IV/HV 关系进行多种函数形式的重复打分；  
- 保持“相对波动”、“价位”、“期限结构”三者的概念独立。

**治理约束**

- 对每个波动子因子，必须能说明：  
  - 与其他波动因子的**差异性**；  
  - 若去除该因子，是否还存在其他因子承担相同信息角色（若有，则视为冗余候选）。  

---

## 5. 动态参数层升级（βₜ / λₜ / αₜ）

### 5.1 输入解耦与高层依赖

**变更点**

1. βₜ、λₜ、αₜ 的输入不再直接使用底层原始字段（如 RelVol、OI_Rank、IV30、HV20、VIX 原值等），而是改为依赖**已构建好的高层聚合因子**：
   - βₜ 仅依赖“订单流强度子因子 + 结构置信度因子”；  
   - λₜ 仅依赖“相对波动主因子 + 期限结构因子”；  
   - αₜ 仅依赖“市场环境/恐慌标签因子”（由 VIX 家族派生）。  

2. 每个动态参数最多允许**两个高层输入**，禁止增加第三第四个来源。

**设计动机**

- 将动态参数从“参与建模”改为“调节高层信号”，避免对原始变量进行二次计分；  
- 明确动态参数的作用范围与影响路径。

**治理约束**

- 必须维护一张“动态参数依赖矩阵”，列出每个参数依赖的高层因子；  
- 审计中若发现动态参数直接读取底层字段，视为违规实现。

### 5.2 参数预算与收缩机制

**变更点**

1. 为 βₜ、λₜ、αₜ 设定**结构性“参数预算”**：
   - 限定其对 DirScore/VolScore 的**最大放大倍数**在一个固定上限内；  
   - 上限为结构参数，不允许回测过程中根据收益调大。  

2. 引入**收缩机制**：
   - 当历史波动状态极端或参数在一定时间窗口内频繁贴近边界时，自动将参数向基准值收缩；  
   - 收缩规则写入版本规范，避免“越调越大”。

3. 动态参数总开关 `enable_dynamic_params` 默认**关闭**，仅在通过结构审计后在生产环境开启。

**设计动机**

- 防止动态参数成为“隐形杠杆”；  
- 强制将动态参数视为**小幅调节器**而非主要 alpha 来源。

**治理约束**

- 配置中必须显式给出：  
  - 参数上/下界；  
  - 最大允许放大系数；  
  - 收缩触发条件（以结构性描述形式）。  
- 任意修改这些数值都视为**版本变更**，需更新版本号与审计记录。

---

## 6. 修正与置信度层升级

### 6.1 单通道置信度机制

**变更点**

1. 将以下原本分散的修正：  
   - 流动性修正；  
   - 结构置信度（单腿/多腿/联动）；  
   - 跨期一致性；  
   - 恐慌环境惩罚；  
   - 缺失数据惩罚；  
   统一整合到一个**单一置信度评分通道**中。

2. 置信度通道输出单一标量 `confidence_score`，仅用于：
   - 映射为置信度等级（高/中/低）；  
   - 控制是否允许输出策略建议（低于某阈值直接 no-trade）；  
   - 控制建议的“仓位强度”标签。  

3. **禁止**修正层直接以乘性方式修改 DirScore / VolScore 数值。

**设计动机**

- 避免“修正链路”成为新的拟合通道；  
- 保持评分值本身的物理含义稳定，把各种“不确定性”集中在置信度维度表达。

**治理约束**

- 审计时需能拆解置信度来源：  
  - 每一类修正对最终置信度的贡献；  
  - 任一修正项的关闭对置信度分布的影响。  
- 任一修正项新增时，必须说明其是否与现有修正项同源，避免重复降权。

---

## 7. 事件模块（Gamma Squeeze 等）升级

### 7.1 事件模块降级为标签层

**变更点**

1. Gamma Squeeze 等“特殊事件模块”保留**检测逻辑**，但统一降级为**事件标签与说明**，包括：
   - 事件类型；  
   - 事件强度（弱/中/强）；  
   - 典型风险轮廓。  

2. 事件对系统的影响仅限于：
   - 对置信度进行**小幅加权**（有上限）；  
   - 在输出中添加风险/机会提示字段。  

3. 明确禁止：
   - 在事件模块内部写入任何“强制策略模板”或“强烈建议某单一结构”的文案；  
   - 通过事件模块直接改写象限判定结果。

**设计动机**

- 保留事件信息价值，但避免事件成为“捷径决策器”；  
- 将事件影响纳入整体置信度框架，而非独立通道。

**治理约束**

- 事件模块必须满足：  
  - 不读写 DirScore/VolScore 原始数值，仅读取其符号与大致区间（如偏多/偏空）；  
  - 不修改象限标签，只输出附加说明。  
- 审计中若发现事件模块输出字段被上游逻辑用作“硬条件”，须回滚。

---

## 8. 决策映射与策略输出升级

### 8.1 中性缓冲带与 no-trade 区域

**变更点**

1. 在 DirScore / VolScore 各自的偏好映射中增加**中性缓冲带**：
   - 当分数接近阈值时，统一映射为“中性偏好 + 低置信度”；  
   - 视为“暂不建议方向性/波动性暴露”的情形。  

2. 将“象限 → 策略模板”的映射拆分为两层：
   - 第一层：仅根据 DirBias / VolBias / Confidence 输出抽象象限标签；  
   - 第二层：具体策略组合（Long Call / Iron Condor 等）由上层策略引擎根据账户规模、风险承受度等决定，不在评分系统内硬编码。

**设计动机**

- 防止边界附近的连续输入造成离散输出剧烈跳变；  
- 将策略结构选择权交还给上层系统，评分模块只做“偏好与置信度”的裁决。

**治理约束**

- 象限映射层**不得**读取任何原始市场字段，只能使用：  
  - DirScore / VolScore；  
  - 置信度等级；  
  - 标的/市场类型（指数/个股等）。  
- 策略模板若需要调整，不应触碰评分模块代码，只调整上层配置。

---

## 9. v2.4 结构治理与审计清单

### 9.1 因子与动态参数审计

1. **因子子空间检查**  
   - 每个子空间的因子数量是否符合约束（1–2 个主因子）；  
   - 是否存在同一字段多次进入不同因子表达的情况。  

2. **动态参数依赖矩阵**  
   - βₜ/λₜ/αₜ 是否仅依赖定义好的高层因子；  
   - 依赖数量是否 ≤ 2；  
   - 放大倍数是否在预算范围内。

### 9.2 修正与置信度通道审计

1. 修正项是否全部通过单一置信度通道生效；  
2. 是否存在任何直接乘以 DirScore / VolScore 的修正逻辑；  
3. 缺失数据是否体现在置信度中，而不是作为“伪造数值”出现在因子输入。

### 9.3 事件模块审计

1. 事件模块是否仅输出标签与辅助信息；  
2. 是否存在事件模块对象限判定的直接覆盖逻辑；  
3. 事件影响是否受明确的上限约束（对置信度的最大修正比例）。

### 9.4 决策映射层审计

1. DirScore/VolScore 轻微扰动（小于设计步长）下，输出象限是否稳定；  
2. 中性缓冲带是否生效（边界附近样本应有显著比例落入“中性/观望”）；  
3. 策略模板是否从评分系统中解耦，仅由上层配置决定。

### 9.5 配置与版本管理审计

1. 动态参数开关 `enable_dynamic_params` 是否与生产配置一致；  
2. 任意阈值/窗口/权重/预算变更是否更新版本号并记录变更说明；  
3. 是否存在绕过配置、在代码中硬写数值的行为。

---

## 10. 结构风险预期

在不引入任何新的收益假设的前提下，v2.4 的结构升级**预期**在以下维度降低风险：

- **因子冗余/共线表达风险**：  
  - 通过子空间重构与因子压缩，预期从“显著”降至“可控”。  

- **隐含调参与数据挖掘空间**：  
  - 通过约束动态参数依赖与预算，引入收缩机制，预期使有效自由度实质下降。  

- **结构性污染源**：  
  - 移除波动率缺失填 0 等做法，改为结构化缺失，预期显著降低异常伪信号。  

- **事件覆盖式决策风险**：  
  - Gamma Squeeze 等事件模块被降级为标签层，决策权回归评分与置信度主链路。  

整体目标是将系统的**结构风险评级**从“高”压降到**“中”这一可治理区间**；  
后续是否真正达成，还需结合 v2.4 实际实现与审计结果进行更新。

---